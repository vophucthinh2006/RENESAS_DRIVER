#include "GPIO.h"

uint8_t GPIO_PortToIndex(GPIO_PORT_t port)
{
    if (port >= '0' && port <= '9')
        return port - '0';
    else if (port >= 'A' && port <= 'B')
        return port - 'A' + 10;
    else
        return 0;
}
